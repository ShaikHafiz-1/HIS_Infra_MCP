apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hospital-mcp-audit-logs
  namespace: hospital-mcp
  labels:
    app: hospital-mcp-server
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard
