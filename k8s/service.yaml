apiVersion: v1
kind: Service
metadata:
  name: hospital-mcp-server
  namespace: hospital-mcp
  labels:
    app: hospital-mcp-server
spec:
  type: ClusterIP
  selector:
    app: hospital-mcp-server
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
